# Embedded in the GRUB EFI image (BR2_TARGET_GRUB2_BUILTIN_CONFIG_EFI).
# GRUB sets $root to the partition it was loaded from (the ESP) and
# $prefix to /EFI/BOOT, so the main config and grubenv are loaded from
# there with no searching.
configfile ${prefix}/grub.cfg
