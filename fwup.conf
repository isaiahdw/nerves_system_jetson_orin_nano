# Firmware configuration file for the NVIDIA Jetson Orin Nano
# (Waveshare JETSON-ORIN-IO-BASE carrier, NVMe boot)
#
# Boot flow: BootROM -> MB1/MB2 -> UEFI (all in module QSPI-NOR, never on
# this disk) -> UEFI's default boot path runs EFI/BOOT/BOOTAA64.efi from
# this disk's ESP, which is GRUB (arm64-efi, built by the system) ->
# grub.cfg boots an attached provisioning stick if one is present,
# otherwise the A/B slot selected by grubenv -> kernel boots via its EFI
# stub with the slot's padded DTB (grub/grub.cfg documents the state
# machine; post-createfs.sh documents the padding).
#
# All boot state lives in EFI/BOOT/grubenv on the ESP, written only by
# fwup (this file and fwup-ops.conf) as whole 1024-byte files from the
# grubenv-* resources. GRUB's one-shot next_entry is the upgrade
# fallback: it is cleared before the new slot is attempted, so a crash +
# watchdog reset boots the validated slot with no counters, UEFI
# variables, or partition renames involved.
#
# Nothing on the disk is signature-checked by the QSPI chain (UEFI
# Secure Boot off).

require-fwup-version="1.12.0"

include("${NERVES_SDK_IMAGES:-.}/fwup_include/fwup-common.conf")

# File resources are listed in the order that they are included in the .fw
# file, which is the order they are written during an update. The grubenv
# resources are last on purpose: an interrupted upgrade must leave the env
# still pointing at the old slot.
# GRUB, built by Buildroot with grub/early.cfg embedded (prefix /EFI/BOOT).
file-resource bootaa64.efi {
    host-path = "${NERVES_SYSTEM}/images/efi-part/EFI/BOOT/bootaa64.efi"
}
file-resource grub.cfg {
    host-path = "${NERVES_SYSTEM}/images/grub.cfg"
}
file-resource usb-boot.cfg {
    host-path = "${NERVES_SYSTEM}/images/usb-boot.cfg"
}
file-resource Image {
    host-path = "${NERVES_SYSTEM}/images/Image"
}
# This must match BR2_LINUX_KERNEL_INTREE_DTS_NAME in nerves_defconfig and
# the devicetree lines in grub/grub.cfg. Written per-slot, so a reverted
# upgrade never leaves the surviving slot with the other slot's device
# tree. Padded with free space by post-createfs.sh: the UEFI firmware
# applies its overlays into the installed blob and fails without room.
file-resource devicetree.dtb {
    host-path = "${NERVES_SYSTEM}/images/tegra234-p3768-0000+p3767-0004-nv.dtb"
}
file-resource rootfs.img {
    host-path = ${ROOTFS}

    # Error out if the rootfs size exceeds the partition size
    assert-size-lte = ${ROOTFS_A_PART_COUNT}
}
# Boot-state variants (1024-byte GRUB environment blocks, see grub/):
#   default-a/b: boot that slot, no one-shot pending (factory, validate)
#   next-a/b:    validated slot stays default, try the named slot once
file-resource grubenv-default-a {
    host-path = "${NERVES_SYSTEM}/images/grubenv-default-a"
}
file-resource grubenv-next-a {
    host-path = "${NERVES_SYSTEM}/images/grubenv-next-a"
}
file-resource grubenv-next-b {
    host-path = "${NERVES_SYSTEM}/images/grubenv-next-b"
}

gpt gpt {
    guid = ${DISK_UUID}

    partition 0 {
        block-offset = ${ESP_PART_OFFSET}
        block-count = ${ESP_PART_COUNT}
        type = ${ESP_PART_TYPE}
        guid = ${ESP_PART_GUID}
        name = "esp"
    }
    partition 1 {
        block-offset = ${BOOT_A_PART_OFFSET}
        block-count = ${BOOT_A_PART_COUNT}
        type = ${BOOT_PART_TYPE}
        guid = ${BOOT_A_PART_GUID}
        name = "boot_a"
    }
    partition 2 {
        block-offset = ${BOOT_B_PART_OFFSET}
        block-count = ${BOOT_B_PART_COUNT}
        type = ${BOOT_PART_TYPE}
        guid = ${BOOT_B_PART_GUID}
        name = "boot_b"
    }
    partition 3 {
        block-offset = ${ROOTFS_A_PART_OFFSET}
        block-count = ${ROOTFS_A_PART_COUNT}
        type = ${LINUX_PART_TYPE}
        guid = ${ROOTFS_A_PART_GUID}
        name = "rootfs_a"
    }
    partition 4 {
        block-offset = ${ROOTFS_B_PART_OFFSET}
        block-count = ${ROOTFS_B_PART_COUNT}
        type = ${LINUX_PART_TYPE}
        guid = ${ROOTFS_B_PART_GUID}
        name = "rootfs_b"
    }
    partition 5 {
        block-offset = ${DATA_PART_OFFSET}
        block-count = ${DATA_PART_COUNT}
        type = ${LINUX_PART_TYPE}
        guid = ${DATA_PART_GUID}
        name = "data"
        expand = true
    }
}

# Provisioning-media GPT: same offsets, but provisioner-specific partition
# names (the stick's kernel mounts root by PARTLABEL, and nothing on the
# stick may resolve to the internal disk or vice versa) and a
# Microsoft-basic-data staging partition so host computers automount it.
# KEEP IN SYNC with `gpt gpt` above.
gpt gpt-provisioner {
    guid = ${PROV_DISK_UUID}

    partition 0 {
        block-offset = ${ESP_PART_OFFSET}
        block-count = ${ESP_PART_COUNT}
        type = ${ESP_PART_TYPE}
        guid = ${PROV_ESP_PART_GUID}
        name = "prov_esp"
    }
    partition 1 {
        block-offset = ${BOOT_A_PART_OFFSET}
        block-count = ${BOOT_A_PART_COUNT}
        type = ${BOOT_PART_TYPE}
        guid = ${PROV_BOOT_A_PART_GUID}
        name = "prov_boot_a"
    }
    partition 2 {
        block-offset = ${BOOT_B_PART_OFFSET}
        block-count = ${BOOT_B_PART_COUNT}
        type = ${BOOT_PART_TYPE}
        guid = ${PROV_BOOT_B_PART_GUID}
        name = "prov_boot_b"
    }
    partition 3 {
        block-offset = ${ROOTFS_A_PART_OFFSET}
        block-count = ${ROOTFS_A_PART_COUNT}
        type = ${LINUX_PART_TYPE}
        guid = ${PROV_ROOTFS_A_PART_GUID}
        name = "prov_rootfs_a"
    }
    partition 4 {
        block-offset = ${ROOTFS_B_PART_OFFSET}
        block-count = ${ROOTFS_B_PART_COUNT}
        type = ${LINUX_PART_TYPE}
        guid = ${PROV_ROOTFS_B_PART_GUID}
        name = "prov_rootfs_b"
    }
    partition 5 {
        block-offset = ${DATA_PART_OFFSET}
        block-count = ${DATA_PART_COUNT}
        type = ${BOOT_PART_TYPE}
        guid = ${PROV_DATA_PART_GUID}
        name = "data"
        expand = true
    }
}

# Location where installed firmware information is stored.
uboot-environment uboot-env {
    block-offset = ${UBOOT_ENV_OFFSET}
    block-count = ${UBOOT_ENV_COUNT}
}

# This firmware task writes everything to the destination media.
# Run at provisioning time.
task complete {
    # Only match if not mounted
    require-unmounted-destination = true

    on-init {
        gpt_write(gpt)

        # Initialize the Nerves environment block (valid CRC, empty) so the
        # uboot_setenv calls below have something to modify. There is no
        # U-Boot to provide defaults; nothing else initializes this block.
        uboot_clearenv(uboot-env)

        fat_mkfs(${ESP_PART_OFFSET}, ${ESP_PART_COUNT})
        fat_setlabel(${ESP_PART_OFFSET}, "ESP")
        fat_mkdir(${ESP_PART_OFFSET}, "EFI")
        fat_mkdir(${ESP_PART_OFFSET}, "EFI/BOOT")

        fat_mkfs(${BOOT_A_PART_OFFSET}, ${BOOT_A_PART_COUNT})
        fat_setlabel(${BOOT_A_PART_OFFSET}, "BOOTA")
        fat_mkdir(${BOOT_A_PART_OFFSET}, "boot")

        # Slot B is formatted but empty at the factory; the first upgrade
        # populates it. grubenv-default-a can never select it.
        fat_mkfs(${BOOT_B_PART_OFFSET}, ${BOOT_B_PART_COUNT})
        fat_setlabel(${BOOT_B_PART_OFFSET}, "BOOTB")
        fat_mkdir(${BOOT_B_PART_OFFSET}, "boot")
    }

    on-resource bootaa64.efi { fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/BOOTAA64.efi") }
    on-resource grub.cfg { fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/grub.cfg") }
    on-resource Image { fat_write(${BOOT_A_PART_OFFSET}, "boot/Image") }
    on-resource devicetree.dtb { fat_write(${BOOT_A_PART_OFFSET}, "boot/tegra234-p3768-0000+p3767-0004-nv.dtb") }
    on-resource rootfs.img {
        # write to the first rootfs partition
        raw_write(${ROOTFS_A_PART_OFFSET})
    }
    on-resource grubenv-default-a {
        fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/grubenv")

        # Include provisioning instructions
        include("${NERVES_PROVISIONING}")

        # Add in the generic Nerves metadata variables. The factory image
        # is trusted by definition: without validated=1 the upgrade tasks
        # (which require it) can never run. sige5 gets this from its
        # initial U-Boot env image; here the env starts from
        # uboot_clearenv, so set it explicitly.
        uboot_setenv(uboot-env, "nerves_fw_active", "a")
        uboot_setenv(uboot-env, "nerves_fw_validated", "1")
        uboot_setenv(uboot-env, "nerves_fw_devpath", ${NERVES_FW_DEVPATH})
        uboot_setenv(uboot-env, "a.nerves_fw_application_part0_devpath", ${NERVES_FW_APPLICATION_PART0_DEVPATH})
        uboot_setenv(uboot-env, "a.nerves_fw_application_part0_fstype", ${NERVES_FW_APPLICATION_PART0_FSTYPE})
        uboot_setenv(uboot-env, "a.nerves_fw_application_part0_target", ${NERVES_FW_APPLICATION_PART0_TARGET})
        uboot_setenv(uboot-env, "a.nerves_fw_product", ${NERVES_FW_PRODUCT})
        uboot_setenv(uboot-env, "a.nerves_fw_description", ${NERVES_FW_DESCRIPTION})
        uboot_setenv(uboot-env, "a.nerves_fw_version", ${NERVES_FW_VERSION})
        uboot_setenv(uboot-env, "a.nerves_fw_platform", ${NERVES_FW_PLATFORM})
        uboot_setenv(uboot-env, "a.nerves_fw_architecture", ${NERVES_FW_ARCHITECTURE})
        uboot_setenv(uboot-env, "a.nerves_fw_author", ${NERVES_FW_AUTHOR})
        uboot_setenv(uboot-env, "a.nerves_fw_vcs_identifier", ${NERVES_FW_VCS_IDENTIFIER})
        uboot_setenv(uboot-env, "a.nerves_fw_misc", ${NERVES_FW_MISC})
        uboot_setenv(uboot-env, "a.nerves_fw_uuid", "\${FWUP_META_UUID}")
    }

    on-finish {
        # Slot B is empty: no b.* metadata (a revert must be impossible
        # until an upgrade populates B). Invalidate rootfs B and the
        # data partition so nothing stale is mistaken for a filesystem.
        raw_memset(${ROOTFS_B_PART_OFFSET}, 256, 0xff)
        raw_memset(${DATA_PART_OFFSET}, 256, 0xff)
    }
}

# Provisioning-media variant of `complete`: same slot-A payload, but with
# the provisioner GPT/labels, the stick boot config, and the data
# partition formatted FAT32 ("STAGING") so a host computer can copy a
# firmware archive onto the written media. This is the offline
# factory-provisioning path in docs/provisioning.md: write a USB drive
# with this task, copy the .fw for the internal disk onto the STAGING
# volume, boot the drive (the internal disk's GRUB defers to it
# automatically), and run fwup against the internal disk from the local
# file.
# KEEP IN SYNC with `task complete` above except where commented.
task complete-provisioner {
    require-unmounted-destination = true

    on-init {
        gpt_write(gpt-provisioner)
        uboot_clearenv(uboot-env)

        fat_mkfs(${ESP_PART_OFFSET}, ${ESP_PART_COUNT})
        fat_setlabel(${ESP_PART_OFFSET}, "PROVESP")
        fat_mkdir(${ESP_PART_OFFSET}, "EFI")
        fat_mkdir(${ESP_PART_OFFSET}, "EFI/BOOT")

        fat_mkfs(${BOOT_A_PART_OFFSET}, ${BOOT_A_PART_COUNT})
        fat_setlabel(${BOOT_A_PART_OFFSET}, "PROVA")
        fat_mkdir(${BOOT_A_PART_OFFSET}, "boot")

        fat_mkfs(${BOOT_B_PART_OFFSET}, ${BOOT_B_PART_COUNT})
        fat_setlabel(${BOOT_B_PART_OFFSET}, "PROVB")
    }

    on-resource bootaa64.efi { fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/BOOTAA64.efi") }
    # DIFFERENCE from `complete`: the stick's boot menu is usb-boot.cfg.
    # It is both the config GRUB on this stick loads directly (as
    # EFI/BOOT/grub.cfg) and the marker file the internal disk's GRUB
    # searches for (as /nerves-usb-boot.cfg, fat_cp'd because fwup
    # streams a resource through fat_write only once).
    on-resource usb-boot.cfg {
        fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/grub.cfg")
        fat_cp(${ESP_PART_OFFSET}, "EFI/BOOT/grub.cfg", "nerves-usb-boot.cfg")
    }
    on-resource Image { fat_write(${BOOT_A_PART_OFFSET}, "boot/Image") }
    on-resource devicetree.dtb { fat_write(${BOOT_A_PART_OFFSET}, "boot/tegra234-p3768-0000+p3767-0004-nv.dtb") }
    on-resource rootfs.img { raw_write(${ROOTFS_A_PART_OFFSET}) }
    on-resource grubenv-default-a {
        # The stick's GRUB path does not read grubenv (usb-boot.cfg is
        # static), but ship a coherent one anyway.
        fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/grubenv")
        include("${NERVES_PROVISIONING}")
        uboot_setenv(uboot-env, "nerves_fw_active", "a")
        uboot_setenv(uboot-env, "nerves_fw_validated", "1")
        uboot_setenv(uboot-env, "nerves_fw_devpath", ${NERVES_FW_DEVPATH})
        uboot_setenv(uboot-env, "a.nerves_fw_application_part0_devpath", ${NERVES_FW_APPLICATION_PART0_DEVPATH})
        uboot_setenv(uboot-env, "a.nerves_fw_application_part0_fstype", ${NERVES_FW_APPLICATION_PART0_FSTYPE})
        uboot_setenv(uboot-env, "a.nerves_fw_application_part0_target", ${NERVES_FW_APPLICATION_PART0_TARGET})
        uboot_setenv(uboot-env, "a.nerves_fw_product", ${NERVES_FW_PRODUCT})
        uboot_setenv(uboot-env, "a.nerves_fw_description", ${NERVES_FW_DESCRIPTION})
        uboot_setenv(uboot-env, "a.nerves_fw_version", ${NERVES_FW_VERSION})
        uboot_setenv(uboot-env, "a.nerves_fw_platform", ${NERVES_FW_PLATFORM})
        uboot_setenv(uboot-env, "a.nerves_fw_architecture", ${NERVES_FW_ARCHITECTURE})
        uboot_setenv(uboot-env, "a.nerves_fw_author", ${NERVES_FW_AUTHOR})
        uboot_setenv(uboot-env, "a.nerves_fw_vcs_identifier", ${NERVES_FW_VCS_IDENTIFIER})
        uboot_setenv(uboot-env, "a.nerves_fw_misc", ${NERVES_FW_MISC})
        uboot_setenv(uboot-env, "a.nerves_fw_uuid", "\${FWUP_META_UUID}")
    }

    on-finish {
        raw_memset(${ROOTFS_B_PART_OFFSET}, 256, 0xff)
        # DIFFERENCE from `complete`: FAT32 staging volume instead of an
        # invalidated (f2fs-to-be) data partition. Fixed 16 GiB
        # filesystem: DATA_PART_COUNT is the tiny pre-expand size, and
        # the filesystem may be smaller than the expanded partition.
        fat_mkfs(${DATA_PART_OFFSET}, 33554432)
        fat_setlabel(${DATA_PART_OFFSET}, "STAGING")
    }
}

task upgrade.a {
    # This task upgrades the A slot
    require-uboot-variable(uboot-env, "nerves_fw_active", "b")

    # Require that the running version of firmware has been validated.
    # If it has not, then failing back is not guaranteed to work.
    require-uboot-variable(uboot-env, "nerves_fw_validated", "1")

    # Verify the expected platform/architecture
    require-uboot-variable(uboot-env, "b.nerves_fw_platform", "${NERVES_FW_PLATFORM}")
    require-uboot-variable(uboot-env, "b.nerves_fw_architecture", "${NERVES_FW_ARCHITECTURE}")

    on-init {
        info("Upgrading slot A")

        # Clear some firmware information just in case this update gets
        # interrupted midway. If this slot was bootable, it's not going to
        # be soon.
        uboot_unsetenv(uboot-env, "a.nerves_fw_version")
        uboot_unsetenv(uboot-env, "a.nerves_fw_platform")
        uboot_unsetenv(uboot-env, "a.nerves_fw_architecture")
        uboot_unsetenv(uboot-env, "a.nerves_fw_uuid")

        # Reformat this slot's boot partition: each slot's boot files live
        # in their own FAT, so this cannot touch the running slot.
        fat_mkfs(${BOOT_A_PART_OFFSET}, ${BOOT_A_PART_COUNT})
        fat_setlabel(${BOOT_A_PART_OFFSET}, "BOOTA")
        fat_mkdir(${BOOT_A_PART_OFFSET}, "boot")

        # Indicate that the entire rootfs partition can be cleared
        trim(${ROOTFS_A_PART_OFFSET}, ${ROOTFS_A_PART_COUNT})
    }

    # Only the rootfs is delta-updated (raw source). The kernel and DTB
    # ship whole in every patch: fwup's FAT-file delta source truncates on
    # fragmented files ("xdelta3 error: source file too short"), and at
    # ~47 MB the pair is noise next to the rootfs.
    on-resource Image { fat_write(${BOOT_A_PART_OFFSET}, "boot/Image") }
    on-resource devicetree.dtb {
        fat_write(${BOOT_A_PART_OFFSET}, "boot/tegra234-p3768-0000+p3767-0004-nv.dtb")
    }
    on-resource rootfs.img {
        delta-source-raw-offset=${ROOTFS_B_PART_OFFSET}
        delta-source-raw-count=${ROOTFS_B_PART_COUNT}
        raw_write(${ROOTFS_A_PART_OFFSET})
    }
    # GRUB refreshes on upgrade so a GRUB fix ships like any other: the
    # ESP files are shared, not per-slot, so this runs while slot B is
    # live - but the file is written completely before the old one is
    # replaced in the FAT, and a boot mid-upgrade boots the old slot via
    # the not-yet-flipped grubenv either way.
    on-resource bootaa64.efi { fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/BOOTAA64.efi") }
    on-resource grub.cfg { fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/grub.cfg") }
    # Written LAST: the moment the boot flips to one-shot-try-A. An
    # interrupted upgrade never reaches this and still boots B.
    on-resource grubenv-next-a {
        fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/grubenv")
    }

    on-finish {
        # Update firmware metadata
        uboot_setenv(uboot-env, "a.nerves_fw_application_part0_devpath", ${NERVES_FW_APPLICATION_PART0_DEVPATH})
        uboot_setenv(uboot-env, "a.nerves_fw_application_part0_fstype", ${NERVES_FW_APPLICATION_PART0_FSTYPE})
        uboot_setenv(uboot-env, "a.nerves_fw_application_part0_target", ${NERVES_FW_APPLICATION_PART0_TARGET})
        uboot_setenv(uboot-env, "a.nerves_fw_product", ${NERVES_FW_PRODUCT})
        uboot_setenv(uboot-env, "a.nerves_fw_description", ${NERVES_FW_DESCRIPTION})
        uboot_setenv(uboot-env, "a.nerves_fw_version", ${NERVES_FW_VERSION})
        uboot_setenv(uboot-env, "a.nerves_fw_platform", ${NERVES_FW_PLATFORM})
        uboot_setenv(uboot-env, "a.nerves_fw_architecture", ${NERVES_FW_ARCHITECTURE})
        uboot_setenv(uboot-env, "a.nerves_fw_author", ${NERVES_FW_AUTHOR})
        uboot_setenv(uboot-env, "a.nerves_fw_vcs_identifier", ${NERVES_FW_VCS_IDENTIFIER})
        uboot_setenv(uboot-env, "a.nerves_fw_misc", ${NERVES_FW_MISC})
        uboot_setenv(uboot-env, "a.nerves_fw_uuid", "\${FWUP_META_UUID}")

        # Reset the validation status; grubenv-next-a (already written)
        # boots A exactly once. Validation promotes it via fwup-ops.
        uboot_setenv(uboot-env, "nerves_fw_active", "a")
        uboot_setenv(uboot-env, "nerves_fw_validated", "0")
        uboot_setenv(uboot-env, "nerves_fw_booted", "0")
        # Scrub bootcount-feature variables this system never uses: a stale
        # upgrade_available masks the real nerves_fw_validated in
        # nerves_runtime's validation heuristic.
        uboot_unsetenv(uboot-env, "upgrade_available")
        uboot_unsetenv(uboot-env, "bootcount")
    }

    on-error {
    }
}

task upgrade.b {
    # This task upgrades the B slot
    require-uboot-variable(uboot-env, "nerves_fw_active", "a")
    require-uboot-variable(uboot-env, "nerves_fw_validated", "1")

    # Verify the expected platform/architecture
    require-uboot-variable(uboot-env, "a.nerves_fw_platform", "${NERVES_FW_PLATFORM}")
    require-uboot-variable(uboot-env, "a.nerves_fw_architecture", "${NERVES_FW_ARCHITECTURE}")

    on-init {
        info("Upgrading slot B")

        # Clear some firmware information just in case this update gets
        # interrupted midway.
        uboot_unsetenv(uboot-env, "b.nerves_fw_version")
        uboot_unsetenv(uboot-env, "b.nerves_fw_platform")
        uboot_unsetenv(uboot-env, "b.nerves_fw_architecture")
        uboot_unsetenv(uboot-env, "b.nerves_fw_uuid")

        fat_mkfs(${BOOT_B_PART_OFFSET}, ${BOOT_B_PART_COUNT})
        fat_setlabel(${BOOT_B_PART_OFFSET}, "BOOTB")
        fat_mkdir(${BOOT_B_PART_OFFSET}, "boot")

        trim(${ROOTFS_B_PART_OFFSET}, ${ROOTFS_B_PART_COUNT})
    }

    # See upgrade.a: rootfs-only delta; kernel and DTB ship whole.
    on-resource Image { fat_write(${BOOT_B_PART_OFFSET}, "boot/Image") }
    on-resource devicetree.dtb {
        fat_write(${BOOT_B_PART_OFFSET}, "boot/tegra234-p3768-0000+p3767-0004-nv.dtb")
    }
    on-resource rootfs.img {
        delta-source-raw-offset=${ROOTFS_A_PART_OFFSET}
        delta-source-raw-count=${ROOTFS_A_PART_COUNT}
        raw_write(${ROOTFS_B_PART_OFFSET})
    }
    # See upgrade.a for the ESP-refresh and env-ordering rationale.
    on-resource bootaa64.efi { fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/BOOTAA64.efi") }
    on-resource grub.cfg { fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/grub.cfg") }
    on-resource grubenv-next-b {
        fat_write(${ESP_PART_OFFSET}, "EFI/BOOT/grubenv")
    }

    on-finish {
        # Update firmware metadata
        uboot_setenv(uboot-env, "b.nerves_fw_application_part0_devpath", ${NERVES_FW_APPLICATION_PART0_DEVPATH})
        uboot_setenv(uboot-env, "b.nerves_fw_application_part0_fstype", ${NERVES_FW_APPLICATION_PART0_FSTYPE})
        uboot_setenv(uboot-env, "b.nerves_fw_application_part0_target", ${NERVES_FW_APPLICATION_PART0_TARGET})
        uboot_setenv(uboot-env, "b.nerves_fw_product", ${NERVES_FW_PRODUCT})
        uboot_setenv(uboot-env, "b.nerves_fw_description", ${NERVES_FW_DESCRIPTION})
        uboot_setenv(uboot-env, "b.nerves_fw_version", ${NERVES_FW_VERSION})
        uboot_setenv(uboot-env, "b.nerves_fw_platform", ${NERVES_FW_PLATFORM})
        uboot_setenv(uboot-env, "b.nerves_fw_architecture", ${NERVES_FW_ARCHITECTURE})
        uboot_setenv(uboot-env, "b.nerves_fw_author", ${NERVES_FW_AUTHOR})
        uboot_setenv(uboot-env, "b.nerves_fw_vcs_identifier", ${NERVES_FW_VCS_IDENTIFIER})
        uboot_setenv(uboot-env, "b.nerves_fw_misc", ${NERVES_FW_MISC})
        uboot_setenv(uboot-env, "b.nerves_fw_uuid", "\${FWUP_META_UUID}")

        # Reset the validation status; grubenv-next-b (already written)
        # boots B exactly once. Validation promotes it via fwup-ops.
        uboot_setenv(uboot-env, "nerves_fw_active", "b")
        uboot_setenv(uboot-env, "nerves_fw_validated", "0")
        uboot_setenv(uboot-env, "nerves_fw_booted", "0")
        uboot_unsetenv(uboot-env, "upgrade_available")
        uboot_unsetenv(uboot-env, "bootcount")
    }

    on-error {
    }
}

task upgrade.unvalidated {
    require-uboot-variable(uboot-env, "nerves_fw_validated", "0")

    on-init {
        error("Please validate the running firmware before upgrading it again.")
    }
}

task upgrade.unexpected {
    require-uboot-variable(uboot-env, "a.nerves_fw_platform", "${NERVES_FW_PLATFORM}")
    require-uboot-variable(uboot-env, "a.nerves_fw_architecture", "${NERVES_FW_ARCHITECTURE}")
    on-init {
        error("Please check the media being upgraded. It doesn't look like either the A or B slots are active.")
    }
}

task upgrade.wrongplatform {
    on-init {
        error("Expecting platform=${NERVES_FW_PLATFORM} and architecture=${NERVES_FW_ARCHITECTURE}")
    }
}

task provision {
    require-unmounted-destination = true
    on-init {
        include("${NERVES_PROVISIONING}")
    }
}
task provision.wrongplatform {
    on-init {
        error("Expecting platform=${NERVES_FW_PLATFORM} and architecture=${NERVES_FW_ARCHITECTURE}")
    }
}
